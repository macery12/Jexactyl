# V2 UI — Completion Audit & Cutover Checklist

> **Purpose:** track which V1 pages/routes have a real V2 equivalent, so we know when
> `/v2` can replace V1 wholesale. This is the running scoreboard — update it as pages land.
>
> **Sources of truth for this audit:**
> - V1 surface to match → [`V1_UI_Map.md`](./V1_UI_Map.md)
> - V2 architecture / what each built section does → [`V2_UI_Structure.md`](./V2_UI_Structure.md)
> - Route registries (the machine-readable "is it built?" signal) → [`ui/src/routes/*.routes.ts`](../ui/src/routes/)
>
> **How "built" is determined:** a registry entry with an `element` renders a real page; without
> one it renders the shared [`Placeholder`](../ui/src/pages/_shared/Placeholder.tsx). Nested sections
> (billing, email, marketplace…) route internally — a few of those sub-routes render a themed
> [`ComingSoon`](../ui/src/pages/admin/billing/ComingSoon.tsx) stub and are marked 🟡 below.

**Legend:** ✅ Done · 🟡 Partial (shell built, some sub-routes/features stubbed) · ⬜ Not started (Placeholder) · ➖ intentionally dropped/merged

---

## Scoreboard

| Area | Done | Partial | Not started | Cutover-ready? |
|---|---|---|---|---|
| **Auth** (`/v2/auth`) | 7 / 7 | — | 0 | ✅ full parity (login, checkpoint, register, forgot/reset, Discord link + register) |
| **Account** (`/v2/account`) | 11 / 11 | — | 0 | ✅ full parity (credentials + orders/invoices landed) |
| **Server** (`/v2/server/:id`) | 10 / 15 | — | 5 | 🟡 Console, Mods, Files, Startup, Network, Custom Domains, Schedules, Users, Settings, AI built |
| **Admin** (`/v2/admin`) | 23 | 0 | 0 | ✅ every page built; Billing stubs (orders/invoices/coupons/exceptions/settings/invoice-settings) landed |

**Bottom line:** the server cockpit is the critical path — the configuration cluster (startup, network, custom-domains, schedules, users, settings) and the AI assistant have landed, leaving 5 of 15 sub-pages (databases, backups, activity, billing, extensions) still as placeholders. V1 cannot be retired until those land. **Admin is now at full page parity.**

---

## 1. Auth — `/v2/auth/*`

Registry: [`auth.routes.ts`](../ui/src/routes/auth.routes.ts)

| V1 route | V2 page | Status | Notes |
|---|---|---|---|
| `login` | [`LoginPage`](../ui/src/pages/auth/LoginPage.tsx) | ✅ | email/password + Turnstile |
| `login/checkpoint` | [`CheckpointPage`](../ui/src/pages/auth/CheckpointPage.tsx) | ✅ | 2FA / recovery code |
| `register`  | [`RegisterPage`](../ui/src/pages/auth/RegisterPage.tsx) | ✅ | username (live availability) + email + password (strength meter + checklist) + Turnstile; auto-login → `/v2`, jGuard pending screen |
| `password` (forgot) | [`ForgotPasswordPage`](../ui/src/pages/auth/ForgotPasswordPage.tsx) | ✅ | server-decided method (`GET /auth/password-reset/method`): email-link or recovery-code |
| `password/reset/:token` | [`ResetPasswordPage`](../ui/src/pages/auth/ResetPasswordPage.tsx) | ✅ | emailed-link landing, sets new password via `/auth/password-reset/reset` |
| `discord/link-choice`  | [`DiscordLinkChoicePage`](../ui/src/pages/auth/DiscordLinkChoicePage.tsx) | ✅ | post-OAuth "account not linked" screen (`GET /auth/modules/discord/registration-data`): shows Discord identity + choose create-new vs link-existing |
| `discord/register`  | [`DiscordRegisterPage`](../ui/src/pages/auth/DiscordRegisterPage.tsx) | ✅ | finish Discord signup: username (live availability via `/auth/modules/discord/check-username`) + SFTP password (strength meter) → `POST /auth/modules/discord/complete` |

---

## 2. Account / Dashboard — `/v2/account/*`

Registry: [`account.routes.ts`](../ui/src/routes/account.routes.ts)

| V1 route | V2 page | Status | Notes |
|---|---|---|---|
| `` (dashboard) | [`DashboardPage`](../ui/src/pages/dashboard/DashboardPage.tsx) | ✅ | server list + stat tiles, activity feed, announcements, account health |
| `` account overview (profile/email/pw) | [`AccountPage`](../ui/src/pages/account/settings/AccountPage.tsx) → Account tab | ✅ | profile, email, password, 2FA, Discord, billing address |
| `security` (2FA + sessions) | AccountPage → Devices tab | ➖✅ | **merged into Settings** — sessions/devices + 2FA live here |
| `credentials` (API keys + SSH keys) | [`CredentialsPage`](../ui/src/pages/account/credentials/CredentialsPage.tsx) | ✅ | tabbed (API / SSH); API keys list + create modal w/ **one-time token reveal** + delete; SSH keys list (SHA256 fingerprint) + add modal + delete. Data via [`credentials.ts`](../ui/src/api/credentials.ts) |
| `activity` | [`ActivityPage`](../ui/src/pages/account/activity/ActivityPage.tsx) | ✅ | |
| `tickets`  | [`TicketsPage`](../ui/src/pages/account/tickets/TicketsPage.tsx) | ✅ | list + new-ticket modal |
| `tickets/:id`  | [`TicketDetailPage`](../ui/src/pages/account/tickets/TicketDetailPage.tsx) | ✅ | thread + reply |
| `billing/order` (store)  | [`StorePage`](../ui/src/pages/account/billing/store/StorePage.tsx) | ✅ | product browse, comparison, featured plan |
| `checkout/configure/:id` | [`ConfigureCheckout`](../ui/src/pages/account/billing/order/ConfigureCheckout.tsx) | ✅ | |
| `checkout/payment` | [`PaymentPage`](../ui/src/pages/account/billing/payment/PaymentPage.tsx) | ✅ | Stripe + PayPal |
| `billing/processing` `/success` `/cancel` | Processing/Success/Cancel pages | ✅ | post-payment states |
| `billing/orders` (order + invoice history)  | [`OrdersPage`](../ui/src/pages/account/billing/orders/OrdersPage.tsx) | ✅ | tabbed (Orders / Invoices); orders table w/ debounced search + advanced filters (processor/status/type/amount/date) + sortable id/total/created + row→detail modal (overview/payment/timeline); invoices table + PDF download. Data via [`orders.ts`](../ui/src/api/orders.ts) |
| `billing/order/:id` (order detail) | Orders → detail modal | ➖✅ | **merged into the Orders table's inspector modal** (matches V1's OrderInspectorModal behavior); no dedicated route |

---

## 3. Server — `/v2/server/:id/*`

Registry: [`server.routes.ts`](../ui/src/routes/server.routes.ts) · **This is the biggest remaining gap.**

| V1 route | V2 page | Status | Notes |
|---|---|---|---|
| `` Console 🔒 | [`ServerOverviewPage`](../ui/src/pages/server/ServerOverviewPage.tsx) | ✅ | ops-grade cockpit: console hero, power/usage/info rail, stat strip, network + activity panels, live websocket |
| `marketplace` (Mods & Plugins) 🔒 | [`MarketplaceSection`](../ui/src/pages/server/marketplace/MarketplaceSection.tsx) | ✅ | mod browser, modpacks, installed, queue |
| `ai`  | [`AiPage`](../ui/src/pages/server/ai/AiPage.tsx) | ✅ | ChatGPT-style assistant: collapsible conversation rail (new chat, open, bookmark-to-keep w/ expiry badge, delete), centered chat column w/ markdown + copyable code blocks, streaming SSE responses w/ stop button, empty-state suggestion chips, Ollama cold-start hint, auto-created + auto-titled conversations (rolling 7-day expiry server-side). Shared chat kit in [`components/ai`](../ui/src/components/ai/) (`ChatMessage`/`ChatMarkdown`/`ChatComposer`/`useChatStream`) reused by the admin playground. Data via [`ai.ts`](../ui/src/api/ai.ts) + [`aiStream.ts`](../ui/src/lib/aiStream.ts) over the existing client AI + conversations API |
| `files` 🔒 | [`FilesSection`](../ui/src/pages/server/files/FilesSection.tsx) | ✅ | file browser (dense table + grid toggle, sort, in-folder filter, breadcrumbs via URL hash), row/mass ops (rename, move, copy, download, delete), new folder, drag-drop upload, SFTP/SSH connection drawer, advanced search (supercharged); CodeMirror editor (view/edit/new) w/ language picker |
| `databases` 🔒 | — | ⬜ | Placeholder |
| `backups` 🔒 | — | ⬜ | Placeholder |
| `schedules` 🔒 | [`SchedulesSection`](../ui/src/pages/server/schedules/SchedulesSection.tsx) | ✅ | list (cards, cron summary, status, next-run) + create/edit modal (name, 5-field cron, active, only-when-online) + run-now + delete; dedicated detail page with ordered tasks editor (power/command/backup actions, time offset, continue-on-failure, add/edit/delete) |
| `users` (subusers) 🔒 | [`UsersSection`](../ui/src/pages/server/users/UsersSection.tsx) | ✅ | list (avatar, 2FA badge, permission count) + add/edit modal with grouped permission matrix (per-group select-all; only editor-held perms grantable) + remove |
| `network` 🔒 | [`NetworkPage`](../ui/src/pages/server/network/NetworkPage.tsx) | ✅ | allocation list (ip:port, alias, notes, primary badge) + set-primary + edit notes + auto-assign (feature-limit gated) + delete |
| `custom-domains` 🔒 | [`CustomDomainsPage`](../ui/src/pages/server/customdomains/CustomDomainsPage.tsx) | ✅ | mapping list (full domain, status badge pending/active/failed, record-type tag, connection hint, last-error, last-synced) + copy address + re-sync (retry provisioning) + delete (removes DNS); **Add domain** modal (available-domain picker, subdomain w/ live `sub.domain` preview, egg-aware SRV/CNAME picker + optional service tag, backend recommendation notice + connection hint). Port auto-taken from primary allocation. `allocation.update`-gated, flag-gated. **`?` help modal.** Data via [`customDomains.ts`](../ui/src/api/customDomains.ts) |
| `startup` 🔒 | [`StartupPage`](../ui/src/pages/server/startup/StartupPage.tsx) | ✅ | flat single-surface layout (tight command+image header, uniform variable tiles) + Docker image dropdown (case-insensitive match, custom-image aware) + egg-variable grid (text/switch/`in:` select, per-field save + invocation patch) + **assisted version picker** (Vanilla/Paper/Forge/Sponge/… via `/startup/versions`, snapshot toggle, optional jar-delete, Save+Reinstall). **Change-egg moving to Billing** |
| `settings` 🔒 | [`SettingsPage`](../ui/src/pages/server/settings/SettingsPage.tsx) | ✅ | general rename+description (settings.rename) + server info (UUID/short-ID/node/image/limits, copy) + reinstall (settings.reinstall, stopped-only, confirm) + billing-gated schedule/cancel deletion. **SFTP lives in Files** (ConnectionPanel), not here |
| `activity` 🔒 | — | ⬜ | Placeholder |
| `billing` 🔒 | — | ⬜ | Placeholder |
| `extensions` 🔒 | — | ⬜ | Placeholder |

---

## 4. Admin — `/v2/admin/*`

Registry: [`admin.routes.ts`](../ui/src/routes/admin.routes.ts)

| V1 route | V2 section | Status | Notes |
|---|---|---|---|
| `` Overview | [`OverviewPage`](../ui/src/pages/admin/overview/OverviewPage.tsx) | ✅ | live admin dashboard: conditional health banner (update/maintenance/install-fail/tickets/billing) + adaptive KPI row (servers/nodes/users/MRR-or-tickets) + 3 panels (fleet health & capacity, work queue, activity feed) + system strip. Polled aggregate `GET /api/application/overview` ([`OverviewController::index`](../app/Http/Controllers/Api/Application/OverviewController.php)) supersedes the thin `/overview/metrics` + `/overview/version` (kept 1 release). Data via [`adminOverview.ts`](../ui/src/api/adminOverview.ts) |
| `settings` | [`SettingsSection`](../ui/src/pages/admin/settings/SettingsSection.tsx) | ✅ | general settings + panel locale. **Panel mode section removed** — its Standard/Debug indicator moved to Features (Personal dropped) |
| `features` | [`FeaturesSection`](../ui/src/pages/admin/features/FeaturesSection.tsx) | ✅ | **NEW.** Replaces the old "Panel mode" section. Per-module on/off grid (custom domains, AI, marketplace, email, webhooks, extensions, tickets) + separate **Billing** master switch + presets (Bare minimum / Restore defaults / Enable everything) + read-only **Panel state** (Standard/Debug w/ env-managed how-to). Disabled features vanish from admin **and** user nav; direct URL access renders a themed [`FeatureDisabled`](../ui/src/pages/_shared/FeatureDisabled.tsx) page via the `FeatureGate` in [`App.tsx`](../ui/src/app/App.tsx). Ships "on except five" (custom domains/AI/mods/email/webhooks off). Live-updates the flags store on save. Data via [`adminFeatures.ts`](../ui/src/api/adminFeatures.ts) → `/api/application/settings/features` ([`FeaturesController`](../app/Http/Controllers/Api/Application/Settings/FeaturesController.php)) |
| `landing` | [`LandingSection`](../ui/src/pages/admin/landing/LandingSection.tsx) | ✅ | section-builder editor + live canvas |
| `theme` | [`ThemeSection`](../ui/src/pages/admin/theme/ThemeSection.tsx) | ✅ | draft + sandboxed preview, presets |
| `nodes` + `servers` (merged) | [`InfrastructureSection`](../ui/src/pages/admin/infrastructure/InfrastructureSection.tsx) | ✅ | ➖ nodes/servers **consolidated** into one section: fleet overview (map/servers/nodes views), node detail cockpit, server detail editor, create/edit/delete, presets. Legacy `nodes/*` `servers/*` redirect in. |
| `users` | [`UsersSection`](../ui/src/pages/admin/users/UsersSection.tsx) | ✅ | list + create/edit modal + delete |
| `roles` | [`RolesSection`](../ui/src/pages/admin/roles/RolesSection.tsx) | ✅ | list + role detail (permission matrix) |
| `tickets` | [`TicketsSection`](../ui/src/pages/admin/tickets/TicketsSection.tsx) | ✅ | staff queue + per-ticket console |
| `email` | [`EmailSection`](../ui/src/pages/admin/email/EmailSection.tsx) | ✅ | overview, SMTP, Resend, testing, notifications, activity, templates, deferred queue |
| `extensions` | [`ExtensionsSection`](../ui/src/pages/admin/extensions/ExtensionsSection.tsx) | ✅ | gallery, manage drawer, repositories, operation progress |
| `marketplace` / `plugins` | [`MarketplaceSection`](../ui/src/pages/admin/marketplace/MarketplaceSection.tsx) | ✅ | overview, settings, per-egg providers |
| `billing` | [`BillingSection`](../ui/src/pages/admin/billing/BillingSection.tsx) | ✅ | Full page parity. **Overview** analytics + **product catalog** (categories + product editor) were already done; now landed: **Orders** ([`OrdersPage`](../ui/src/pages/admin/billing/orders/OrdersPage.tsx)) — dense table, smart search (`#`id/`@`user/`txn:`/`cap:`/`pid:`/`pay:`), advanced filters (processor/status/type/amount/date + transaction lookup), status-tinted rows, threat pill, row→inspector modal (overview/customer/payment/threat-breakdown via `/orders/{id}/threat`). **Invoices** — table + search/status filter + download/resend/regenerate/void. **Coupons** — table + create/edit modal + delete. **Exceptions** — table + resolve/resolve-all. **Settings** ([`SettingsPage`](../ui/src/pages/admin/billing/settings/SettingsPage.tsx)) — general (currency, cooldown, legal links, require-address, config import/export) **+ folded-in** Billing Rules (multiplier steps + node pricing) **+** Payment Integrations (Stripe keys, PayPal/Link enable + credentials, disable-module) — V1's standalone `billing-rules`/`integrations` sidebar pages are sections here. **Invoice Settings** — company/tax/logo, numbering, retention, storage driver (local/s3/r2) + R2 usage meter + test-connection, editorChrome sticky-save. Data via [`adminBilling*.ts`](../ui/src/api/) over existing `/api/application/billing/*`. **Scoped-lighter:** config-import uses the direct path (override off, ignore-dupes on) rather than V1's full nest/egg conflict-resolution dialog; Stripe/PayPal setup *guide* walkthroughs not ported (key entry is). |
| `activity`  | [`AdminActivityPage`](../ui/src/pages/admin/activity/AdminActivityPage.tsx) | ✅ | admin-scoped feed (actor avatar, severity dots, category/API badges) in a capped column beside a sticky filter rail — search + user + event + sort + clear (full V1 `ActivityContainer` parity); click-to-inspect metadata modal |
| `api` (API keys) | [`ApiKeysSection`](../ui/src/pages/admin/api/ApiKeysSection.tsx) | ✅ | list (identifier, memo, allowed IPs, last used, created) + create modal (memo + 9 resource permission grants) w/ one-time token reveal + delete confirm |
| `developers/api-docs` | [`ApiDocsPage`](../ui/src/pages/admin/apidocs/ApiDocsPage.tsx) | ✅ | Native themed OpenAPI reference (master–detail): search + method filter, endpoints grouped **Client API / Application API** → tags, per-endpoint param/body/response schema tables, auth badge, copy-as-cURL. Spec tools: download JSON, regenerate cache, open raw Stoplight. Reads `/api/openapi.json` via [`apiDocs.ts`](../ui/src/api/apiDocs.ts) + [`openapi.ts`](../ui/src/pages/admin/apidocs/openapi.ts). Backend: daemon (`api/remote`) already hidden; now also excludes `api/webhooks` + `api/storefront` internal receivers (`config/api-docs.php` `exclude_prefixes`) |
| `auth` (JGuard) | [`AuthSection`](../ui/src/pages/admin/auth/AuthSection.tsx) | ✅ | in-page rail (Modules / jGuard Settings / Pending). **Modules:** registration toggle, security (force-2FA + attempt limit), captcha (provider + Turnstile keys), onboarding, Discord/Google SSO cards (client id/secret + callback + remove), spruced-up **Add module** modal. **jGuard:** approval mode/delay/pending-message + disable; **Pending accounts** table (approve/reject, live time-remaining). Data via [`adminAuth.ts`](../ui/src/api/adminAuth.ts) |
| `custom-domains` | [`CustomDomainsSection`](../ui/src/pages/admin/customdomains/CustomDomainsSection.tsx) | ✅ | in-page rail (Domains / API Keys / Settings). **Domains:** catalog CRUD (domain, Cloudflare zone id, API-key link, allowed nests/eggs checklists, default + per-egg SRV service tags, wildcard, enabled) w/ restriction summary badges. **API Keys:** Cloudflare token pool CRUD (write-only token, enable toggle, delete-blocked-if-assigned). **Settings:** module enable, fallback CF token, wildcard policy (allow + max/user), rate limits (create/sync/billing-options per min) + disabled-state notice. **`?` operator help modal.** Data via [`adminCustomDomains.ts`](../ui/src/api/adminCustomDomains.ts) over existing `/api/application/custom-domains` |
| `ai` | [`AiSection`](../ui/src/pages/admin/ai/AiSection.tsx) | ✅ | Tabbed cockpit (Overview / Playground / Logs / Settings). **Overview:** connection card (cheap cached health check, re-test), stat tiles (24h / 7d w/ cache hits / all-time success + latency), 7-day activity bars, source breakdown, top users, recent requests. **Playground:** streaming admin chat (shared chat kit). **Logs:** filterable request log (search/source/status, 500 cap, `cached` ⚡ badge). **Settings:** provider mode, endpoint, API key set/remove, model w/ live **Ollama model auto-discovery** (sizes) + presets, max-tokens/temperature sliders, **keep_alive + scheduled warm-up** (Ollama), feature toggles (live-updates flags store), system prompt w/ counter, test + save. Data via [`adminAi.ts`](../ui/src/api/adminAi.ts). **Backend upgrades:** response cache (1h, streamed replays, `cached` usage-log column), models-listing health check instead of a real generation (cached 5 min, `?fresh=1`), `GET /ai/models` discovery endpoint, `p:ai:warm` every-5-min scheduler keeping the Ollama model resident |
| `webhooks` | [`WebhooksSection`](../ui/src/pages/admin/webhooks/WebhooksSection.tsx) | ✅ | in-page rail (Configuration / Event Management), gated by module enable screen. **Configuration:** endpoint URL save (hidden "configured" indicator), send test webhook, disable module, integration guide + event-format sample. **Events:** stat tiles (total/enabled %/disabled/categories), search, global enable/disable-all + test, collapsible per-category groups with enable/disable-all, full-card-clickable per-event toggles (optimistic). Data via [`webhooks.ts`](../ui/src/api/webhooks.ts) |
| `alerts` | [`AlertsSection`](../ui/src/pages/admin/alerts/AlertsSection.tsx) | ✅ | Master–detail workspace: left rail lists alerts (type dot, position, priority, off-badge) + right editor pane with **live preview** (renders the alert as banner/slide-out/popup/notification, themed by type). Full form (title, content, type, position, scope [hidden for `notification`], priority, enabled, dismissible, link+text, **user targeting** all/specific w/ debounced user search + chips, start/end schedule), sticky save bar, delete confirm. Data via [`adminAlerts.ts`](../ui/src/api/adminAlerts.ts) over existing `/api/application/alerts`. |
| `databases` | [`DatabasesSection`](../ui/src/pages/admin/databases/DatabasesSection.tsx) | ✅ | Master–detail database-hosts workspace: left rail lists hosts (live reachability dot + `host:port` address) with name/host search; right editor pane (Connection: name/host/port · Credentials: username/password [blank keeps stored]) with sticky save bar, copy-address, create + delete confirm. Full V1 parity for `/admin/databases` (list/search/create/edit/delete). Data via [`adminDatabases.ts`](../ui/src/api/adminDatabases.ts) over existing `/api/application/databases`. |
| `nests` (+ eggs editor) | [`NestsSection`](../ui/src/pages/admin/nests/NestsSection.tsx) | ✅ | Master–detail nests workspace (rail + edit + delete + eggs table, create/import egg) + full tabbed egg editor (About/Docker/Variables/Install/Advanced) with per-tab save, create mode, drag-reorder variables + rules builder, export/delete. Data via [`adminNests.ts`](../ui/src/api/adminNests.ts) |

---

## 5. Cross-cutting systems (not pages, but cutover-relevant)

| System | Status | Notes |
|---|---|---|
| Route registry → routing + nav | ✅ | single source of truth, permission + flag gating carried over |
| Bootstrap (`window.*` → Zustand) | ✅ | same Blade→JS handoff as V1 |
| Theming (semantic CSS-var model) | ✅ | runtime-injected, admin-editable |
| i18n (Paraglide, 13 locales) | ✅ | build-time key checking; per-user locale picker still TODO |
| Data layer (TanStack Query + axios/CSRF) | ✅ | reuses existing Laravel API — no backend changes required by V2 so far |
| Serving at `/v2` | ✅ | `templates/v2/core.blade.php` |

---

## 6. Remaining work to retire V1 (the punch list)

**Hard blockers — V1 has these, V2 must before cutover:**
- [x] Server: **Files** manager (incl. file edit/view) ✅ — browser + CodeMirror editor, SFTP/SSH drawer, advanced search. Scoped out for now: chmod / compress / decompress.
- [ ] Server: Databases, Backups, Schedules, Subusers, Network, Startup, Settings
- [x] Server: **Custom Domains** ✅ — mapping list + add/re-sync/delete, egg-aware SRV/CNAME, `?` help modal
- [ ] Server: Activity, Billing, Extensions
- [x] Server: **AI Assistant** ✅ — ChatGPT-style chat + conversation history, streaming, shared chat kit
- [x] Auth: Register, Forgot/Reset password (email + recovery-code) ✅ · [x] Discord link/register ✅ — **Auth area at full parity**
- [x] Auth: surface `users.recovery_code` ✅ — code is now **hashed** (was reversible `Crypt`), shown once on the register screen (copy/download), and regenerated via a password-gated modal at `/v2/account/settings` (Recovery code card). Backend: `verify()` uses `Hash::check`; new `RecoveryCodeController` (`GET`/`POST`/`acknowledge` under `/api/client/account/recovery-code`); `recovery_code_seen` now driven for the "not saved" nudge. **Note:** pre-existing users' legacy encrypted codes no longer validate — they must regenerate from settings.
- [x] Account: **API keys + SSH keys** (V1 `credentials`) ✅ — tabbed page, one-time token reveal, SHA256 fingerprints
- [x] Account: Orders + invoice history (`billing/orders`, `billing/order/:id`) ✅ — filterable/sortable orders table + detail modal + invoices w/ PDF download. **Account area now at full parity.**
- [x] Admin: Overview dashboard ✅ — live polled aggregate (`GET /api/application/overview`): health banner + adaptive KPIs + fleet/queue/activity panels
- [ ] Admin: prune legacy `/overview/metrics` + `/overview/version` **at V1 cutover** — still consumed by V1 `OverviewContainer.tsx` (both) and `ExtensionsContainer.tsx` (version); safe to delete only once `resources/scripts` is retired
- [x] Admin: finish Billing stubs (orders, invoices, coupons, exceptions, settings, invoice-settings) ✅ — full page parity; V1 `billing-rules`/`integrations` folded into Settings sections. Follow-up: port the config-import conflict-resolution dialog + Stripe/PayPal setup guides if needed.
- [x] Admin: Nests + Eggs editor
- [x] Admin: Auth (JGuard) ✅ · Webhooks ✅ · Alerts ✅ · Custom Domains ✅ · Databases ✅ · AI ✅

**Non-blocking / verify before flipping the switch:**
- [ ] Per-user locale picker (panel default already works)
- [ ] Confirm every 🟡/➖ merge (security→settings, nodes+servers→infrastructure, order-detail→orders modal) preserves all V1 capabilities
- [ ] Sweep for V1 routes not represented in any V2 registry
